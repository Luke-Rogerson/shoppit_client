const defaultState = {
  user: {},
  itemCategories: {},
  items: {},
  itemsByCategory: {}
};

const entities = (state = defaultState, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default entities;
