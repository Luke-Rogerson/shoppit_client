const defaultState = {
  users: {},
  categories: {},
  items: {}
};

const entities = (state = defaultState, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default entities;
