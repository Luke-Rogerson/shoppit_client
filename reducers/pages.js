const defaultState = {
  pages: {
    friends: {
      friendsList: [],
      isLoading: false
    }
  }
};

const pages = (state = defaultState, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default pages;
